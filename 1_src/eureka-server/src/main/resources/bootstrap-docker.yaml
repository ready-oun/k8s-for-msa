spring:
  application:
    name: eureka-server
  profiles:
#    active: local/
    active: docker
  cloud:
    config:
      uri: http://config-server:8080
    loadbalancer:
      ribbon:
        enabled: false