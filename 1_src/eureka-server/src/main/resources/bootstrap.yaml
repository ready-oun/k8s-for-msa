spring:
  application:
    name: eureka-server
  profiles:
    active: local
#    active: docker
  cloud:
    config:
      uri: http://localhost:8080
    loadbalancer:
      ribbon:
        enabled: false