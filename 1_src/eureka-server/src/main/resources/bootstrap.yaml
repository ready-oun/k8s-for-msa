spring:
  application:
    name: eureka-server
  profiles:
    active: docker
#    activate:
#      on-profile: localhost
#    active: docker
  cloud:
    config:
#      uri: http://localhost:8888
      uri: http://config-server:8888
    loadbalancer:
      ribbon:
        enabled: false