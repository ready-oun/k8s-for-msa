spring:
  profiles: docker
  application:
    name: factorial-cache
  cloud:
    config:
      uri: http://config-server:8080
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
