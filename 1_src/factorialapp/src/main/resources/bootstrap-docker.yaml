spring:
  profiles: docker
  application:
    name: factorial-app
  cloud:
    config:
      uri: http://config-server:8888
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
